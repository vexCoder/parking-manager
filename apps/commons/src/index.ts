export enum LogType {
  USER = "USER",
  DEVICE = "DEVICE",
}

export enum LogAction {
  ENTER = "ENTER",
  EXIT = "EXIT",
  VACANT = "VACANT",
  OCCUPIED = "OCCUPIED",
}

export enum UserType {
  ADMIN = "ADMIN",
  USER = "USER",
}

export enum DeviceStatus {
  VACANT = "VACANT",
  OCCUPIED = "OCCUPIED",
}